import React from 'react';
import './Avatar.scss';

const Logo = ({ img, alt }) => {
  return (
    <div className="avatar">
      <a className="avatarLink">
        
      </a>
    </div>
  );
};

export default Logo;
